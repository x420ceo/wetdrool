{
  "name": "@wetdrool/feed-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Replaceable, explainable WetDrool feed-ranking service",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist coverage",
    "dev": "node ../../scripts/run-scoped-runtime.mjs --rate-limit -- tsx watch src/server.ts",
    "lint": "eslint src test",
    "start": "node dist/src/server.js",
    "test": "vitest run --exclude 'dist/**'",
    "test:coverage": "vitest run --coverage --exclude 'dist/**'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "11.3.0",
    "@fastify/rate-limit": "11.2.0",
    "@wetdrool/config": "workspace:*",
    "@wetdrool/rate-limit": "workspace:*",
    "fastify": "5.12.4",
    "zod": "4.6.2"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "tsx": "4.23.1",
    "vitest": "4.1.10"
  }
}
